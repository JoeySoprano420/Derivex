module core.math

function clamp(val:f64, min:f64, max:f64) -> f64
    if val < min then return min
    if val > max then return max
    return val
end

function percent_of(base:f64, percent:f64) -> f64
    return (base * percent) / 100.0
end
